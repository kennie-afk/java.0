# All values below are placeholders. Replace every one with a real, unique
# secret before applying this manifest to any cluster — see k8s/README.md.
apiVersion: v1
kind: Secret
metadata:
  name: smartre-secrets
  namespace: smartre
type: Opaque
stringData:
  JWT_SECRET: "placeholder"
  DB_PASSWORD: "placeholder"
  INTERNAL_SECRET: "placeholder"
  MPESA_CONSUMER_KEY: "placeholder"
  MPESA_CONSUMER_SECRET: "placeholder"
  MPESA_PASSKEY: "placeholder"
  MPESA_CALLBACK_URL: "https://your-domain.com/api/payments/mpesa/callback"

