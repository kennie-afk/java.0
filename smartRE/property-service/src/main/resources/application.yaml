spring:
  application:
    name: property-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://property-db:5432/property_db}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driverClassName: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      retries: 3
      properties:
        enable.idempotence: true
    consumer:
      group-id: ${spring.application.name}
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: earliest
      properties:
        spring.json.trusted.packages: "com.kenyarealestate.*"
        spring.json.use.type.headers: false
        spring.json.value.default.type: com.kenyarealestate.property.kafka.Events$VerificationApprovedEvent

server:
  port: 8083

jwt:
  secret: ${JWT_SECRET}

services:
  verification-url: ${VERIFICATION_SERVICE_URL:http://verification-service:8082}
  user-service-url: ${USER_SERVICE_URL:http://user-service:8081}
  internal-secret: ${INTERNAL_SECRET}
  gateway-public-url: ${PUBLIC_API_URL:http://localhost:8080}
  s3-public-base-url: ${S3_PUBLIC_BASE_URL:https://smartre-documents.s3.amazonaws.com}

gateway:
  signing-secret: ${GATEWAY_SIGNING_SECRET}

redis:
  search-cache-ttl-seconds: 120
  search-cache-prefix: "property:search:"
  property-cache-prefix: "property:detail:"
  property-detail-ttl-seconds: 300
  view-debounce-window-minutes: 30

kafka:
  topics:
    verification-events: verification-events
    property-events: property-events

management:
  endpoint:
    health:
      probes:
        enabled: true
      show-details: when-authorized
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  metrics:
    export:
      prometheus:
        enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

resilience4j:
  circuitbreaker:
    instances:
      verification-service:
        registerHealthIndicator: true
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        automaticTransitionFromOpenToHalfOpenEnabled: true
        waitDurationInOpenState: 10s
        failureRateThreshold: 50
        eventConsumerBufferSize: 10
  retry:
    instances:
      verification-service:
        maxAttempts: 3
        waitDuration: 500ms
        enableExponentialBackoff: true
        exponentialBackoffMultiplier: 2

# Per-service throttling, behind the gateway's own limiter. See RateLimitProperties for
# why these are looser than the gateway's: this catches traffic that did not come through
# the front door, not ordinary traffic that did.
rate-limit:
  enabled: ${RATE_LIMIT_ENABLED:true}
  capacity: ${RATE_LIMIT_CAPACITY:300}
  refill-per-minute: ${RATE_LIMIT_REFILL_PER_MINUTE:300}
  write-capacity: ${RATE_LIMIT_WRITE_CAPACITY:60}
  write-refill-per-minute: ${RATE_LIMIT_WRITE_REFILL_PER_MINUTE:60}
  trust-forwarded-for: ${RATE_LIMIT_TRUST_FORWARDED_FOR:true}
