name: Backend CI

on:
  push:
    branches: [main]
    paths: ['smartRE/**']
  pull_request:
    branches: [main]
    paths: ['smartRE/**']

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service:
          - api-gateway
          - user-service
          - verification-service
          - property-service
          - viewing-service
          - payment-service
          - review-service
          - notification-service
          - property-management-service
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin
          cache: maven

      - name: Build and test ${{ matrix.service }}
        working-directory: smartRE/${{ matrix.service }}
        run: mvn -B verify

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-reports-${{ matrix.service }}
          path: smartRE/${{ matrix.service }}/target/surefire-reports
          if-no-files-found: ignore
